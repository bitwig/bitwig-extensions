package com.bitwig.extensions.controllers.mcu.value;

/**
 * Handles in converting a string into a different format.
 */
public interface StringValueConverter {
    String convert(String displayValue);
}
