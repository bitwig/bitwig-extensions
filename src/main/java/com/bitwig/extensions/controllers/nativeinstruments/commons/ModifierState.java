package com.bitwig.extensions.controllers.nativeinstruments.commons;

public enum ModifierState {
   NONE,
   DUPLICATE,
   SELECT,
   SOLO,
   MUTE,
   ERASE,
   SHIFT,
   ARM,
   STOP,
   VARIATION;
}
